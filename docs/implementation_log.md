# Implementation Log

This file is updated at the end of every chunk so a future session can resume from repository facts instead of chat history.

## Chunk 0 - Project Skeleton & Safety

Status: completed

### Scope

- Create a minimal strict TypeScript project.
- Add Vitest smoke coverage.
- Add safe ignore rules for generated files, credentials, WhatsApp auth/session state, local data, and logs.
- Add local configuration with no credentials.
- Add project documentation under `docs/`.
- Do not install or use Baileys yet.

### Files Changed

- `.gitignore`: added excludes for Node dependencies, build output, environment files, WhatsApp auth/session artifacts, local data/logs, IntelliJ metadata, the existing `.venv/`, and `auth_old/`.
- `package.json`: added strict TypeScript/Vitest project scripts and dev dependencies. No Baileys dependency was added.
- `package-lock.json`: generated by `npm.cmd install`.
- `tsconfig.json`: strict NodeNext TypeScript configuration.
- `src/config/AppConfig.ts`: local non-secret config with default timezone and local generated paths.
- `src/index.ts`: minimal application entry point and smoke-testable startup summary.
- `test/unit/config.test.ts`: Vitest smoke tests.
- `README.md`: prerequisites and command reference.
- `docs/documentation_instructions.md`: documentation rules for future sessions.
- `docs/project_state.md`: current status and resume instructions.
- `docs/file_guide.md`: explanation of created files.
- `docs/bug_log.md`: open/resolved bug tracker.
- `docs/WhatsApp_Send_Later_Codex_Implementation_Plan_HE.docx`: copied source plan document.

### Commands Run

- `node --version` -> `v24.18.0`
- `npm --version` failed in PowerShell because `npm.ps1` is blocked by execution policy. Use `npm.cmd`.
- `npm.cmd --version` -> `11.16.0`
- `npm.cmd install` -> passed; 55 packages installed, 0 vulnerabilities. npm warned that `esbuild@0.28.2` has an install script not yet covered by `allowScripts`; verification still passed.
- `npm.cmd run typecheck` -> passed.
- `npm.cmd test` -> passed; 1 test file, 2 tests.
- `npm.cmd run build` -> passed.
- `git status --short --ignored` -> generated `node_modules/` and `dist/` are ignored. No `auth/`, `data/`, `logs/`, or session artifacts are present.

### Verification

- Node major version is >= 20.
- Install passed.
- Typecheck passed.
- Tests passed.
- Build passed.
- No lint script exists in Chunk 0.
- No WhatsApp auth/session/data artifacts are tracked or present.

### Manual Test

- Not required for Chunk 0.

### Known Limitations

- No WhatsApp connection or sending exists yet.
- Existing IntelliJ files were already staged before Chunk 0. `.gitignore` now excludes `.idea/` and `*.iml` for future safety, but already staged files remain staged unless the user asks to unstage/remove them.

## Chunk 1 - Baileys Connection + QR

Status: completed

### Scope

- Install Baileys connection dependencies.
- Add `WhatsAppAdapter` abstraction with `connect()`, `disconnect()`, and `getStatus()`.
- Add `BaileysWhatsAppAdapter` using `useMultiFileAuthState` under the gitignored `auth/` path.
- Register `connection.update` and `creds.update` handlers.
- Render QR through `qrcode-terminal`; do not use deprecated `printQRInTerminal`.
- Add `npm.cmd run wa:connect` CLI.
- Add mock-based tests for connection status transitions, reconnect decisions, and event handler registration.
- Do not implement message sending.

### Package Versions Inspected

- `@whiskeysockets/baileys@7.0.0-rc14`
- `@hapi/boom@10.0.1`
- `qrcode-terminal@0.12.0`
- `@types/qrcode-terminal@0.12.2`

Installed Baileys typings confirmed:

- `connection.update` event uses `Partial<ConnectionState>`.
- `creds.update` event exists.
- `useMultiFileAuthState(folder)` returns `{ state, saveCreds }`.
- `DisconnectReason.loggedOut` is `401`.
- Baileys package requires Node `>=20.0.0`.

### Files Changed

- `package.json`: added `wa:connect` script and Chunk 1 dependencies.
- `package-lock.json`: updated dependency lockfile.
- `src/whatsapp/WhatsAppAdapter.ts`: public WhatsApp transport abstraction.
- `src/whatsapp/BaileysConnectionState.ts`: pure Baileys-to-internal status mapping.
- `src/whatsapp/BaileysWhatsAppAdapter.ts`: Baileys implementation, QR rendering, credential save handler, and status logging.
- `src/cli/waConnect.ts`: manual QR connection CLI.
- `test/unit/BaileysConnectionState.test.ts`: state transition tests.
- `test/unit/BaileysReconnect.test.ts`: reconnect decision tests.
- `test/unit/BaileysEventHandlers.test.ts`: event registration and QR forwarding tests.
- `README.md`: added current Chunk 1 scope and QR link command.
- `docs/implementation_log.md`: added this Chunk 1 log.
- `docs/project_state.md`: updated current status and next manual gate.
- `docs/file_guide.md`: added Chunk 1 files.
- `docs/bug_log.md`: added npm install warning note.

### Commands Run

- `npm.cmd install @whiskeysockets/baileys @hapi/boom qrcode-terminal` -> passed; 0 vulnerabilities. npm warned about install scripts requiring approval coverage for Baileys/protobuf/esbuild.
- `npm.cmd install -D @types/qrcode-terminal` -> passed; 0 vulnerabilities. Same npm install-script approval warning.
- `npm.cmd list @whiskeysockets/baileys @hapi/boom qrcode-terminal @types/qrcode-terminal --depth=0` -> confirmed versions above.
- Inspected installed Baileys package metadata and `.d.ts` files under `node_modules/@whiskeysockets/baileys`.
- `npm.cmd run typecheck` -> passed.
- `npm.cmd test` -> passed; 4 test files, 13 tests.
- `npm.cmd run build` -> passed.
- `git status --short --ignored` -> generated `node_modules/` and `dist/` are ignored.
- `Get-ChildItem -Force auth,data,logs -ErrorAction SilentlyContinue` -> no such directories yet.

### Verification

- Typecheck passed.
- Unit tests passed:
  - QR event maps to `awaiting_qr`.
  - `connection=open` maps to `connected`.
  - logged-out close maps to `logged_out`.
  - temporary close maps to `reconnect_needed`.
  - `creds.update` handler is registered and calls `saveCreds`.
  - QR update is rendered through the injected QR handler and forwarded to status handling.
- Build passed.
- No lint script exists.
- No real WhatsApp auth/session artifacts were created during automated verification.

### Manual QR Test

- Completed on 2026-08-24.
- User confirmed the terminal printed `WhatsApp connection is open.`
- User confirmed WhatsApp on the phone shows a new linked device.
- Screenshot also showed the QR prompt, a temporary close, reconnect attempt 1/3, and a successful open connection.
- QR payloads, auth files, phone/account details, and message contents remain sensitive and must not be pasted into chat or committed.

### Known Limitations

- Manual QR linking was confirmed by the user on 2026-08-24. The terminal showed `WhatsApp connection is open.` and the phone showed a new linked device.
- No message sending exists yet.
- A bounded reconnect loop exists for temporary closes, with `reconnect_needed` surfaced before retry.
- `auth/` is created by the manual WhatsApp link flow. It is gitignored and must stay untracked.
- `creds.update` save errors are not yet surfaced through a structured logger; this is acceptable for Chunk 1 but should be revisited when logging is introduced.

## Chunk 2 - Send Now

Status: completed

### Scope

- Extend `WhatsAppAdapter` with `sendText(recipient, text)`.
- Add internal `SendResult` with `success`, `providerMessageId`, `errorCode`, and `retryable`.
- Add a separate recipient normalizer for explicit phone-number recipients.
- Validate before transport: recipient is not empty, text is not empty, groups are rejected.
- Add a one-shot CLI: `npm.cmd run wa:send -- --to <number> --text "..."`.
- Do not add scheduler, retries, contacts, groups, UI, or bulk sending.

### Baileys API Inspected

- Installed `@whiskeysockets/baileys@7.0.0-rc14` typings define `sendMessage(jid, content, options?)`.
- `sendMessage` returns `Promise<WAMessage | undefined>`.
- The provider message id is available as `message?.key.id` when Baileys returns it.

### Files Changed

- `package.json`: added `wa:send` script.
- `src/whatsapp/WhatsAppAdapter.ts`: added `NormalizedRecipient`, `SendResult`, send error codes, and `sendText`.
- `src/whatsapp/RecipientNormalizer.ts`: added phone-number normalization to WhatsApp individual JIDs and validation for empty, invalid, and group recipients.
- `src/whatsapp/SendTextNow.ts`: added validation-first send service that catches transport exceptions and returns `SendResult`.
- `src/whatsapp/BaileysWhatsAppAdapter.ts`: implemented `sendText` through Baileys `socket.sendMessage`.
- `src/cli/waSend.ts`: added one-shot send CLI that connects, waits for `connected`, sends once, and disconnects.
- `test/unit/RecipientNormalizer.test.ts`: added normalizer coverage for `+972`, spaces, hyphens, and group rejection.
- `test/unit/SendTextNow.test.ts`: added empty-text validation, exactly-one adapter call, and transport exception mapping tests.
- `README.md`: documented send-now command and manual test warning.
- `docs/project_state.md`: moved state to Chunk 2 implementation verified with manual send pending.
- `docs/file_guide.md`: documented Chunk 2 source and test files.
- `docs/implementation_log.md`: added this Chunk 2 log.

### Commands Run

- Inspected the source plan document for Chunk 2 acceptance criteria.
- Inspected installed Baileys `.d.ts` files under `node_modules/@whiskeysockets/baileys`.
- `npm.cmd run typecheck` -> passed.
- `npm.cmd test` -> passed; 6 test files, 19 tests.
- `npm.cmd run build` -> passed.

### Verification

- Typecheck passed.
- Unit tests passed:
  - normalizer handles a leading `+972` country code.
  - normalizer removes spaces and hyphens while preserving the country code digits.
  - group recipients are rejected before transport.
  - empty text is rejected without calling the adapter.
  - a valid send request calls the adapter exactly once.
  - transport exceptions are translated to `SendResult` with `errorCode=transport_error` and `retryable=true`.
- Build passed.
- No lint script exists.

### Manual Send Test

- Completed on 2026-08-25.
- User confirmed one real test message sent successfully.
- The real recipient and message text are intentionally not recorded.
- Do not paste real phone numbers, message contents, QR payloads, auth files, or account details into chat.

### Known Limitations

- No scheduler exists yet.
- No retries exist yet; transport failures are reported as retryable but not retried in Chunk 2.
- Groups and arbitrary JIDs are intentionally rejected.
- Phone-number validation is conservative and expects country-code digits.

## Chunk 3 - Persistent Session & Reconnect

Status: automated implementation complete; manual restart/send verification pending

### Scope

- Extract connection lifecycle and reconnect timers into a `ConnectionManager`.
- Use bounded reconnect backoff for temporary disconnects: 1s, 2s, 5s, 10s, 30s, with small jitter by default.
- Stop reconnect loops for relink-required Baileys closes.
- Add graceful shutdown for `SIGINT` and `SIGTERM`.
- Report `creds.update` save failures through structured logging instead of fire-and-forget.
- Do not add scheduler, retries, contacts, groups, UI, or bulk sending.

### Baileys API Inspected

- Installed `@whiskeysockets/baileys@7.0.0-rc14` exports `DisconnectReason`.
- Available disconnect codes include `connectionClosed`, `connectionLost`, `connectionReplaced`, `timedOut`, `loggedOut`, `badSession`, `restartRequired`, `multideviceMismatch`, `forbidden`, and `unavailableService`.
- There is no literal `revoked` enum in this installed version; relink-required handling maps `loggedOut`, `connectionReplaced`, `badSession`, `multideviceMismatch`, and `forbidden` to internal `needs_relink`.

### Files Changed

- `src/whatsapp/WhatsAppAdapter.ts`: replaced `logged_out` status with `needs_relink` and added structured `WhatsAppLogEvent`/`WhatsAppLogger` types.
- `src/whatsapp/BaileysConnectionState.ts`: added relink-required classification and updated reconnect decisions.
- `src/whatsapp/ConnectionManager.ts`: added centralized lifecycle, bounded backoff, jitter, reconnect exhaustion logging, and shutdown timer cancellation.
- `src/whatsapp/BaileysWhatsAppAdapter.ts`: delegated lifecycle to `ConnectionManager`, kept Baileys socket open/close responsibilities, and reported credential-save failures through sanitized structured logs.
- `src/cli/gracefulShutdown.ts`: added shared CLI shutdown handler for `SIGINT` and `SIGTERM`.
- `src/cli/waConnect.ts`: uses shared graceful shutdown.
- `src/cli/waSend.ts`: uses shared graceful shutdown and treats `needs_relink` as the relink-required status.
- `test/unit/ConnectionManager.test.ts`: added fake-timer reconnect and shutdown tests.
- `test/unit/BaileysConnectionState.test.ts`: updated relink-required status expectation.
- `test/unit/BaileysReconnect.test.ts`: added no-reconnect coverage for terminal/relink-required disconnect codes.
- `test/unit/BaileysEventHandlers.test.ts`: added credential-save error reporting coverage.
- `README.md`, `docs/project_state.md`, `docs/file_guide.md`, and `docs/bug_log.md`: updated for Chunk 3.

### Commands Run

- Inspected the source plan document for Chunk 3 acceptance criteria.
- Inspected installed Baileys disconnect reason typings and runtime enum under `node_modules/@whiskeysockets/baileys`.
- `npm.cmd run typecheck` -> failed once during implementation because `BaileysWhatsAppAdapter` still had a stale `this.status = "connecting"` assignment after lifecycle extraction.
- `npm.cmd run typecheck` -> passed after removing the stale assignment.
- `npm.cmd test` -> failed once because the new credential-save error test asserted before the async rejection handler ran.
- `npm.cmd test` -> passed after fixing the test wait; 7 test files, 24 tests.
- `npm.cmd run build` -> passed.

### Verification

- Typecheck passed.
- Unit tests passed:
  - temporary disconnects use the expected 1s, 2s, 5s fake-timer backoff sequence.
  - relink-required closes do not reconnect.
  - shutdown cancels reconnect timers and closes the active connection.
  - `creds.update` invokes credential persistence.
  - credential persistence failures are forwarded to a structured error handler.
  - Baileys terminal/relink-required close codes do not reconnect-loop.
- Build passed.
- No lint script exists.

### Manual Restart Test

- Pending user verification.
- Required steps:
  1. Run `npm.cmd run wa:connect`.
  2. Confirm it reaches `WhatsApp connection is open.` without displaying a new QR.
  3. Stop the process with Ctrl+C.
  4. Run one send-now test again with a test number or consenting recipient:
     `npm.cmd run wa:send -- --to <country-code-number> --text "test message"`
  5. Confirm the message appears once on both sender and receiver sides.
- Do not paste real phone numbers, message contents, QR payloads, auth files, or account details into chat.

### Known Limitations

- Chunk 3 does not add the scheduler or send retry policy.
- Reconnect backoff is for WhatsApp socket lifecycle only; message retry behavior is still a later chunk.
- True exactly-once WhatsApp delivery still cannot be guaranteed without provider-side idempotency.
- Chunk 4 must not start until the manual restart/no-new-QR/send test is confirmed.
